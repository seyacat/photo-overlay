{
  "name": "photo-overlay",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "postbuild": "npm run copy-files",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run copy-files & tsc && node dist/server.js",
    "nodemon": "nodemon --watch src --ext .ts,.html --exec npm start",
    "copy-files": "copyfiles -u 1 src/lib/* src/**/*.html src/**/*.png dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@swc/core": "^1.7.28",
    "@types/express": "^5.0.0",
    "copyfiles": "^2.4.1",
    "ts-node": "^10.9.2",
    "tsup": "^8.3.0",
    "typescript": "^5.6.2",
    "typescript-bundle": "^1.0.18",
    "uglify-js": "^3.19.3"
  },
  "dependencies": {
    "express": "^4.21.0",
    "mathjs": "^13.1.1",
    "nodemon": "^3.1.7"
  }
}
